{
  "name": "panma-harness",
  "version": "0.0.17",
  "description": "Multi-agent supervisor harness for Claude Code. Auto-dispatches role-based subagents (designer, executors, verifier, rule-applier) with Ralph loop, retry budget, and project-defined domain executors. Also writes a per-turn session title sidecar that panma-hud reads to keep the statusline name in sync with the current request.",
  "author": {
    "name": "panma"
  },
  "repository": "https://github.com/panma-claude/harness",
  "keywords": [
    "claude-code",
    "plugin",
    "multi-agent",
    "orchestration",
    "harness"
  ],
  "commands": "./commands/"
}
